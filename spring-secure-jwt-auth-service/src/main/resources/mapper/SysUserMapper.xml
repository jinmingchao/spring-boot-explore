<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bidpages.mapper.SysUserMapper">

    <!--    Map<String, Object> selectUserById(Long id);-->
    <select id="selectSysUserByUserName" resultType="com.bidpages.mdm.entity.SysUser">
            select * from sys_user where user_name = ${userName} limit 1
    </select>

</mapper>